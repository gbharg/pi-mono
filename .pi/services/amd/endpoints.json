{
  "base_urls": {
    "pm_rest_api": "https://pm-api-137.advancedmd.com",
    "identity_app": "https://static-100.advancedmd.com",
    "saml_api": "https://api-100.advancedmd.com",
    "login_marketing": "https://login.advancedmd.com",
    "note": "pm-api-137 was discovered from network traffic on 2026-04-04. The numeric suffix is assigned per-customer-shard; expect it to differ for other offices. api-100 and static-100 use shard 100 which appears to be a multi-tenant default."
  },
  "auth": {
    "identity_app_framework": "Angular SPA at static-100.advancedmd.com/apps/identity",
    "flow": [
      "1. POST /api/singlesignon/saml/uservalidationrequests/{officeKey}/{user} (api-100) -> 204 / 200",
      "2. GET /api/locator/defaulturls?officeKey={key} (api-100) -> returns pm-api host for this office (e.g. pm-api-137)",
      "3. POST /api/authentication/preauth/status2fa (pm-api) -> returns pre-auth token (authorizationToken)",
      "4. POST /api/authentication/preauth/send2faemail (pm-api) -> triggers email 2FA delivery",
      "5. POST /api/authentication/token/access (pm-api) with grant_type=http://advancedmd.com/oauth/grant-type/mfa-totp, code=<6-digit>, authorizationToken=<preauth> -> returns final bearer token",
      "6. Final bearer is used as Authorization: Bearer <token> against pm-api-{shard}.advancedmd.com/api/*"
    ],
    "auth_header": "Authorization: Bearer <token>",
    "additional_header": "amd-office-key: 161112 (per hipaa-agent client code; unverified against live API)",
    "token_ttl": "unknown, short-lived; refresh via /api/authentication/token/refresh (guess)",
    "gotchas": [
      "2FA code is delivered by EMAIL to the logged-in user, not SMS/TOTP",
      "Pre-auth authorizationToken has its own grant_type: http://advancedmd.com/oauth/grant-type/mfa-totp",
      "400 response body is a raw stack trace string, not JSON",
      "saml-session cookie at api-100.advancedmd.com is set pre-auth and is NOT a usable bearer",
      "The Angular SPA clears localStorage on logout but leaves marketing cookies; presence of advancedmd.com cookies is NOT proof of active session"
    ]
  },
  "office_key": "161112",
  "office_name": "Exult Healthcare",
  "discovered_at": "2026-04-05T06:15:00Z",
  "discovery_sources": [
    "Network traces captured from Playwright Chrome DevTools session (/tmp/amd_st_login_out.txt, /tmp/claude-502/.../blhyjsmoz.output)",
    "hipaa-agent-work client code at /tmp/hipaa-agent-work/hipaa_agent/integrations/amd_{connect,fhir}_client.py",
    "amd_metrics.py script at /tmp/amd_metrics.py (unrun — no live token)",
    "amd_patients.json dataset at /tmp/amd_patients.json (975 records, pulled 2026-04-02)"
  ],
  "categories": {
    "auth": [
      {
        "method": "POST",
        "path": "/api/singlesignon/saml/uservalidationrequests/{officeKey}/{user}",
        "host": "api-100.advancedmd.com",
        "purpose": "Validate username+office combination pre-login",
        "params": {"officeKey": "string", "user": "string (uppercased login name)"},
        "response_shape": {"204": "no content on valid", "200": "validation details"},
        "example_curl": "curl -X POST 'https://api-100.advancedmd.com/api/singlesignon/saml/uservalidationrequests/161112/GAUTAM'",
        "confidence": "observed",
        "rate_limit": "unknown"
      },
      {
        "method": "GET",
        "path": "/api/locator/defaulturls",
        "host": "api-100.advancedmd.com",
        "purpose": "Locate the assigned pm-api shard for an office",
        "params": {"officeKey": "string"},
        "response_shape": {"urls": "object mapping service to host (e.g. pmApi -> pm-api-137.advancedmd.com)"},
        "example_curl": "curl 'https://api-100.advancedmd.com/api/locator/defaulturls?officeKey=161112'",
        "confidence": "observed",
        "notes": "This is the service-discovery endpoint. Call FIRST when bootstrapping a new office — you cannot hard-code pm-api host."
      },
      {
        "method": "POST",
        "path": "/api/authentication/preauth/status2fa",
        "host": "pm-api-{shard}.advancedmd.com",
        "purpose": "Check 2FA status for a login attempt; returns pre-auth token for subsequent MFA calls",
        "body": {"grant_type": "http://advancedmd.com/oauth/grant-type/mfa-totp", "code": "<authorizationToken UUID>", "client_id": "AdvancedMD.Authentication"},
        "response_shape": {"authorizationToken": "uuid", "preferredMethodType": "EMAIL|SMS|TOTP", "daysLeft": "int"},
        "confidence": "observed"
      },
      {
        "method": "POST",
        "path": "/api/authentication/preauth/send2faemail",
        "host": "pm-api-{shard}.advancedmd.com",
        "purpose": "Trigger 2FA email delivery",
        "body": {"email": "string", "request": {"grant_type": "...", "code": "<authorizationToken>", "client_id": "AdvancedMD.Authentication"}},
        "confidence": "observed"
      },
      {
        "method": "POST",
        "path": "/api/authentication/token/access",
        "host": "pm-api-{shard}.advancedmd.com",
        "purpose": "Exchange 2FA code + pre-auth token for final bearer token",
        "body": {"grant_type": "http://advancedmd.com/oauth/grant-type/mfa-totp", "code": "<6-digit email code>", "authorizationToken": "<preauth uuid>", "client_id": "AdvancedMD.Authentication"},
        "response_shape": {"access_token": "string (bearer)", "expires_in": "int seconds", "refresh_token": "string"},
        "confidence": "observed (but only 400 Invalid Code responses captured so far)",
        "gotchas": ["400 body is a raw C# stack trace string, not JSON"]
      }
    ],
    "patients": [
      {
        "method": "GET",
        "path": "/api/patients",
        "host": "pm-api-{shard}.advancedmd.com",
        "purpose": "List/search patients for an office",
        "params": {
          "officeid": "string (required) - office key",
          "offset": "int",
          "limit": "int (observed: 500 works)",
          "firstappointmentstart": "date YYYY-MM-DD (filter: new patients whose first appt is >= date)",
          "firstappointmentend": "date YYYY-MM-DD",
          "createddatefrom": "date YYYY-MM-DD (guess — try as fallback)",
          "createddateto": "date YYYY-MM-DD (guess)"
        },
        "response_shape": {
          "totalcount": "int (sometimes)",
          "count": "int (sometimes)",
          "patients": "array of Patient",
          "data": "array (alt shape)"
        },
        "response_fields_sample": [
          "id: int",
          "licensekey: int (office)",
          "licensename: string",
          "name: string (LAST, FIRST)",
          "firstname: string",
          "lastname: string",
          "chartnumber: string",
          "ssn: string (often blank)",
          "dob: iso datetime",
          "ispatientactive: bool",
          "gender: M|F",
          "zipcode: string",
          "homephone: string",
          "email: string",
          "defaultepisodeid: int",
          "defaultcopayamount: float",
          "profileid: int (provider panel)",
          "profilecode: string (e.g. BHAR00)",
          "responsiblepartyid: int",
          "hipaarelationship: string (numeric code)"
        ],
        "confidence": "observed",
        "gotchas": [
          "Response envelope shape inconsistent: sometimes totalcount, sometimes count, sometimes raw array",
          "ssn field returns whitespace-padded string when present but masked",
          "patient size (demographics only): ~890 bytes / ~222 tokens mean",
          "firstappointmentstart filter works; createddatefrom is UNVERIFIED"
        ],
        "rate_limit": "5 req/s per hipaa-agent client config, unverified"
      }
    ],
    "appointments": [
      {
        "method": "GET",
        "path": "/api/appointments",
        "host": "pm-api-{shard}.advancedmd.com",
        "purpose": "Query appointments by date range and status",
        "params": {
          "officeid": "string (required)",
          "startdate": "date YYYY-MM-DD",
          "enddate": "date YYYY-MM-DD",
          "aptstatus": "int (1=Scheduled, 2=Complete, 3=NoShow, 4=Cancelled, 5=Rescheduled — from amd_metrics.py, UNVERIFIED)",
          "offset": "int",
          "limit": "int"
        },
        "response_shape": {"totalcount": "int", "appointments": "array"},
        "confidence": "guess",
        "gotchas": ["Status codes come from amd_metrics.py's author notes, not documented spec. Verify before trusting."]
      }
    ],
    "fhir": [
      {
        "method": "GET",
        "path": "/fhir/v1/Patient/{id}",
        "host": "pm-api-{shard}.advancedmd.com (assumed)",
        "purpose": "FHIR R4 patient read",
        "auth": "Bearer + amd-office-key header",
        "content_type": "application/fhir+json",
        "confidence": "guess (from hipaa-agent-work client code, not verified against live API)"
      },
      {
        "method": "GET",
        "path": "/fhir/v1/Patient",
        "purpose": "FHIR R4 patient search",
        "params": {"name": "string", "birthdate": "YYYY-MM-DD"},
        "confidence": "guess"
      },
      {
        "method": "GET",
        "path": "/fhir/v1/Appointment",
        "purpose": "FHIR R4 appointment search",
        "params": {"patient": "string", "date": "ge|leYYYY-MM-DD (chainable)"},
        "confidence": "guess"
      },
      {
        "method": "GET",
        "path": "/fhir/v1/Condition",
        "purpose": "FHIR R4 condition list",
        "params": {"patient": "string"},
        "confidence": "guess"
      }
    ],
    "connect_v1": [
      {
        "method": "POST",
        "path": "/connect/v1/appointments",
        "purpose": "Create appointment (WRITE — requires approval)",
        "body": {"patientId": "string", "providerId": "string", "startTime": "iso8601", "durationMinutes": "int", "visitType": "string"},
        "response_shape": {"appointmentId": "string"},
        "confidence": "guess"
      },
      {
        "method": "PATCH",
        "path": "/connect/v1/appointments/{id}",
        "purpose": "Update appointment (WRITE)",
        "confidence": "guess"
      },
      {
        "method": "POST",
        "path": "/connect/v1/appointments/{id}/cancel",
        "purpose": "Cancel appointment (WRITE)",
        "body": {"reason": "string"},
        "confidence": "guess"
      },
      {
        "method": "POST",
        "path": "/connect/v1/charges",
        "purpose": "Create charge (WRITE)",
        "body": {"appointmentId": "string", "cptCodes": "array", "diagnosisCodes": "array"},
        "confidence": "guess"
      },
      {
        "method": "GET",
        "path": "/connect/v1/patients/{id}/balance",
        "purpose": "Get patient financial balance",
        "response_shape": {"totalBalance": "float", "insuranceBalance": "float", "patientBalance": "float", "lastPaymentDate": "iso", "lastPaymentAmount": "float"},
        "confidence": "guess"
      }
    ],
    "ppmd_legacy": [
      {
        "name": "PPMDResults handler",
        "purpose": "Legacy PPMD command API — returns the 'success/api/rolename/toolbarcode/version' blob seen in localStorage key PPMDResults_{officeKey}_{user}",
        "observed_response_sample": "{\"s\":\"advancedmd-api-processrequest-7-6d8cc9f847-qgqkh\",\"lst\":\"04/04/2026 22:33:31\",\"Results\":{\"success\":\"1\",\"api\":\"0\",\"rolename\":\"ADMIN\",\"toolbarcode\":\"standard\",\"version\":\"26.1\"}}",
        "confidence": "observed (localStorage artifact only, exact endpoint unknown)",
        "notes": "This suggests an older REST endpoint at /ppmd or /processrequest that returns the classic PPMD response envelope. Worth probing once auth works."
      }
    ]
  },
  "NOT_DISCOVERED": [
    "eligibility / insurance verification endpoints",
    "claims / billing status endpoints",
    "documents / clinical attachments",
    "messaging / patient portal",
    "tasks / todo lists",
    "audit log endpoints",
    "provider / staff directory",
    "setup / configuration endpoints",
    "encounters (SOAP note / visit note) endpoints"
  ],
  "next_steps_when_auth_live": [
    "1. Call GET /api/locator/defaulturls to confirm pm-api shard for the office",
    "2. Try GET /openapi.json, /swagger.json, /swagger/index.html on the pm-api host — many .NET APIs expose Swashbuckle by default",
    "3. Hit /api/patients?officeid=161112&limit=1 to sanity-check bearer",
    "4. Probe /api/eligibility, /api/claims, /api/messages, /api/tasks, /api/documents — common .NET controller names",
    "5. Check the Angular bundle at https://static-100.advancedmd.com/apps/pm/main.*.js for baked-in API paths"
  ]
}
