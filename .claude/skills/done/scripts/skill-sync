#!/bin/bash
# TODO(AI-158): Implement skill-sync (done-local wrapper)
# Referenced by: done/SKILL.md
# Purpose: Wrapper that invokes the canonical skill-sync script
#   at ~/openclaw/scripts/skill-sync from the /done workflow context.
#   Distributes skills from repo source to agent target directories.
exec "$(dirname "$0")/../../../scripts/skill-sync" "$@"
